{
  "name": "agendamento-eecjvs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build && cd ../backend && npm install && npm run build:tsc",
    "start": "node backend/dist/index.js",
    "db:migrate": "cd backend && npm run db:migrate"
  }
}
